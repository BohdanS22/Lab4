name: Deploy to S3

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: AKIAVYV52HD5JSQKLIYD
        aws-secret-access-key: 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
        aws-region: us-east-1

    - name: Sync files to S3
      run: aws s3 sync . s3://cmtr-a9mdgrbi-bucket-1751114344 --delete --acl public-read --exclude "https://github.com/BohdanS22/Lab4.git"
